specVersion: "v1"
serviceId: "safexcode-master"
location: "asia-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "safexcode-master-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
